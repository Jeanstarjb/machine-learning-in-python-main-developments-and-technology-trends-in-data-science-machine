apiVersion: v1
kind: Service
metadata:
  name: inference-service
  labels:
    app: ml-inference
spec:
  selector:
    app: ml-inference
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: LoadBalancer